{% if not conversation_context %}
This is the FIRST turn. Greet the user briefly and ask ONE simple question.

Example: "Hi! What kind of movies do you enjoy?"

Generate a brief greeting with ONE question (max 10 words total):
{% else %}
Generate ONE short question (max 15 words) about: {{ entities[0] if entities else 'movies' }}

{% if conversation_context %}
Recent conversation:
{{ conversation_context.split('\n')[-4:] | join('\n') }}
{% endif %}

{% if discovered_preferences and discovered_preferences.get('liked') %}
They already mentioned liking: {{ discovered_preferences['liked'][:3] | join(', ') }}
{% endif %}

Ask ONE direct question about "{{ entities[0] if entities else 'their preferences' }}". No follow-ups.
Question:{% endif %}